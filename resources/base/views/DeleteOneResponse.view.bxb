result-view {
  match: DeleteOneResponse (this)

  render {
    nothing
  }

  message ("#{value(this)}")

  conversation-drivers {
    conversation-driver {
      template-macro (CV_LIST_ALL_MEMORIES)
    }
  }
}
