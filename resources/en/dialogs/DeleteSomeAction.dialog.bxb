dialog (Confirmation) {
  match: DeleteSomeAction
  if ($handsFree) {
    template("Are you sure you want to delete everything that matched?")
  } else {
    template("Are you sure you want to delete what was shown?")
  }
}
