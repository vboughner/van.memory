result-view {
  match: RecallLastMemoryResponse (this) {
  }

  message ("#{value(this.speech)}")

  render {
    if (this.success) {
      layout-macro (memory-summary) {
        param (memory) {
          expression (this.memory)
        }
      }
    } else {
      nothing
    }
  }

  conversation-drivers {
    conversation-driver {
      template ("List all memories")
    }
  }
}
